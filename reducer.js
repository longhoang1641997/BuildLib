const init = {
    cars: ['BMW']
}

export default function reducer(state = init, action, value) {

    switch (action) {
        case 'add': 
            break;
        default:
            return state
    }
}